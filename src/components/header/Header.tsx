import React, { FC } from 'react';
import './Header.css';

const Header: FC = () => {
    return (
        <div className="container">
            <h1 className="header">React TypeScript GraphQL ToDo list</h1>
        </div>
    );
};

export default Header;
